{
	"name": "pandora-api",
	"version": "0.1.0",
	"private": true,
	"license": "LGPL-3.0-or-later",
	"engines": {
		"node": ">=24.11.x"
	},
	"type": "module",
	"exports": {
		"./api": {
			"types": "./dist/api/index.d.ts",
			"default": "./dist/api/index.js"
		}
	},
	"files": [
		"/dist/**/*"
	],
	"scripts": {
		"clean": "rimraf dist coverage",
		"build": "rimraf dist && tsc -p ./tsconfig.json",
		"dev": "rimraf dist && tsc -p ./tsconfig.json -w --skipLibCheck",
		"lint": "eslint --max-warnings 0 .",
		"lint:fix": "eslint --fix .",
		"type-check": "pnpm run '/type-check:.*/'",
		"type-check:test": "tsc -p ./test/tsconfig.json --noEmit",
		"type-check:src": "tsc -p ./tsconfig.json --noEmit",
		"test": "jest -c ../jest.config.js --selectProjects pandora-api",
		"test:coverage": "jest -c ../jest.config.js --selectProjects pandora-api --coverage",
		"test:watch": "jest -c ../jest.config.js --selectProjects pandora-api --watch",
		"prepack": "pnpm run build"
	},
	"dependencies": {
		"async-lock": "1.4.1",
		"socket.io-client": "4.8.3"
	},
	"devDependencies": {
		"@jest/globals": "30.4.1",
		"@types/async-lock": "1.4.2",
		"@types/node": "24.13.3",
		"pandora-common": "workspace:*",
		"rimraf": "6.1.3",
		"tslib": "2.8.1",
		"typescript": "7.0.2"
	},
	"peerDependencies": {
		"pandora-common": "workspace:*"
	}
}
