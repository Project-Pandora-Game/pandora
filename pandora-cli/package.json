{
	"name": "pandora-cli",
	"version": "0.1.0",
	"private": true,
	"license": "LGPL-3.0-or-later",
	"engines": {
		"node": ">=24.x"
	},
	"type": "module",
	"bin": {
		"pandora-cli": "./dist/index.js"
	},
	"files": [
		"/dist/**/*"
	],
	"scripts": {
		"pandora-cli": "node --enable-source-maps ./dist/index.js",
		"clean": "rimraf dist coverage",
		"build": "rimraf dist && tsc -p ./tsconfig.json",
		"dev": "rimraf dist && tsc -p ./tsconfig.json -w --skipLibCheck",
		"lint": "eslint --max-warnings 0 .",
		"lint:fix": "eslint --fix .",
		"type-check": "pnpm run '/type-check:.*/'",
		"type-check:test": "tsc -p ./test/tsconfig.json --noEmit",
		"type-check:src": "tsc -p ./tsconfig.json --noEmit",
		"test": "jest -c ../jest.config.js --selectProjects pandora-cli",
		"test:coverage": "jest -c ../jest.config.js --selectProjects pandora-cli --coverage",
		"test:watch": "jest -c ../jest.config.js --selectProjects pandora-cli --watch",
		"prepack": "pnpm --filter pandora-common run build && pnpm --filter pandora-api run build && pnpm run build"
	},
	"dependencies": {
		"pandora-api": "workspace:*",
		"pandora-common": "workspace:*"
	},
	"devDependencies": {
		"@jest/globals": "30.4.1",
		"@types/node": "24.13.3",
		"rimraf": "6.1.3",
		"tslib": "2.8.1",
		"typescript": "6.0.3"
	}
}
